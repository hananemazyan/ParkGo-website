.home .home-box nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 65px;
    background: linear-gradient(rgba(109, 29, 236, 0.2),rgba(149, 26, 225, 0.2));
    backdrop-filter: blur(1px);
    border-radius: .7rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 4rem;
    z-index: 10;
}

/* Adjust content padding to prevent overlap */
.home .content {
    padding-top: 65px; /* Add padding equal to navbar height */
}

/* Adjust logo and menu styles for sticky navbar */
.home .home-box nav .logo h3 {
    color: var(--white-color);
    font-weight: 400;
    margin: 0; /* Remove margin for h3 inside logo */
}

.home .home-box nav .logo .bar {
    display: flex; /* Ensure bar icon is always visible */
}

.home .home-box nav .menu {
    flex-grow: 1; /* Allow menu to take remaining space */
    text-align: center; /* Center menu items */
}

.home .home-box nav .signup-login {
    display: flex;
    align-items: center;
    gap: .5rem;
    margin-right: 4rem; /* Add right margin to align with content */
}

/* Adjust menu and signup-login styles for sticky navbar */
.home .home-box nav .menu ul {
    display: flex;
    gap: 3rem;
    list-style: none;
    margin: 0; /* Remove default margin */
}

.home .home-box nav .menu ul li {
    margin: 0; /* Remove default margin for list items */
}

.home .home-box nav .signup-login a {
    color: var(--white-color);
    padding: .3rem 1rem;
    border-radius: 2rem;
    transition: all 0.3s ease; /* Add smooth transition */
}

/* Sticky navbar styles on scroll */
.sticky {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Optional: Add shadow on scroll */
}